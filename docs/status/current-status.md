# Admin Go/Vue Runtime Current Status

状态更新时间：2026-06-07

本文是当前运行时事实入口，只放最新关键事实、验证缺口和跳转。详细 per-module 分组明细已经分层到 `docs/status/module-matrix.md`；历史变更记录已经归档到 `docs/status/archive/2026-05-runtime-change-log.md`。

不要把 planned 写成 implemented。文档与运行时冲突时，以 live runtime、smoke/test output、served API 和 process config 为准。

## Current status layers

```text
2026-06-07 Admin Vue AI run monitor tab lifecycle hotfix verified: `src/views/Main/ai/runs/index.vue` now lets `el-tab-pane lazy` own the stats tab lifecycle instead of wrapping `RunStats` in an extra `activeTab` `v-if`, and `src/views/Main/ai/runs/components/RunStats/index.vue` keeps the empty Top-N `el-table` sections mounted instead of unmounting them when cleared `ai_runs` data returns empty; `tests/shared/ai/ai-run-unified-records.test.ts` guards the stable lifecycle shape, and `npm test` plus `npm run typecheck -- --noEmit` passed
2026-06-07 Unified AI run records verified: `internal/module/ai/run` now owns one provider-attempt monitor across Admin chat, Canvas text, Admin/Canvas image, and Canvas video; `ai_runs` records `platform/modality/source_type/source_id/input_snapshot/usage_status`, chat-only message links are nullable by design, non-chat rows do not fake `ai_conversations` or `ai_messages`, Canvas free-generation still does not write `ai_billing_records`, Admin Vue run monitor consumes the new fields through filters/table/detail, and guards reject nullable/optional `source_id`/`input_snapshot` frontend contracts; verification passed `admin_back_go: go test ./... -count=1 -p=1` with `GOMAXPROCS=2`, `admin_front_ts: npm test`, `admin_front_ts: npm run typecheck -- --noEmit`, `admin_back_go/scripts/check-contract.ps1`, `git diff --check`, `scripts/check-runtime-doc-facts.ps1`, and `scripts/check-agent-governance.ps1 -Mode working`
2026-06-07 Admin Vue AI image payload source-quality cleanup verified: `src/api/ai/images.ts` now uses explicit `optionalImageEnum(...)` and `optionalPositiveID(...)` instead of `payload.* || undefined` and truthy mask ID skips; `tests/shared/ai/ai-image-api.test.ts` guards the slice, and source-quality inventory now reports 280 source files, 0 `any` candidates, 0 `as any` candidates, 0 `catch(...: any)` candidates, 555 fallback candidates, and 0 direct external HTTP candidates; this closes only AI image create-task payload fallback debt
2026-06-07 Codex-first quality runway added: `docs/architecture/09-codex-first-quality-runway.md` now ties current Go route/API drift, live MySQL schema, Admin Vue source-quality inventory, Canvas Next contract/performance rules, and agent selection into one narrow-slice hardening route; this is navigation/governance only, not a claim that fallback/performance/capability hardening is complete
2026-06-07 Admin Vue demo any source-quality cleanup verified: `src/views/Main/component/form/index.vue`, `src/views/Main/component/display/index.vue`, and `src/views/Main/component/effect/components/ParticleBackground.vue` no longer use tracked `any` rows or hidden numeric `|| 1` fallbacks; `tests/shared/form/form-demo-source-quality.test.ts`, `tests/shared/display/display-demo-source-quality.test.ts`, and `tests/shared/effect/particle-background-source-quality.test.ts` guard the slice, and source-quality inventory now reports 280 source files, 0 `any` candidates, 0 `as any` candidates, 0 `catch(...: any)` candidates, 555 fallback candidates, and 0 direct external HTTP candidates; this closes only current demo any debt and leaves fallback rows as review inventory
docs/status/current-status.md                         # 当前入口：关键事实、验证缺口、读法
docs/status/module-matrix.md                          # 当前明细：per-module Go/Vue runtime status
docs/status/known-issues.md                           # 当前已知 bug / WIP 证据，不当成 verified
docs/status/archive/2026-05-runtime-change-log.md     # 历史证据：2026-05 verified change log
```

## Current runtime truth

- 架构方向：new-system-first / multi-platform-first。后端目标是一套核心能力服务 admin / app / canvas / openapi / merchant 等多个前端或平台入口。
- 当前 backend boundary truth 是 `internal/module/{capability}/transport/{platform}` + `internal/shared` + `internal/infra`；active exceptions 是产品范围决定，不是架构漂移。
- Admin behavior preservation 仍是验收标准：现有 admin URLs、DB 表名、permission codes、i18n keys、route metadata、operation log rules、queue task types、payment callback/finalizer 行为、frontend typed API contracts 不得被重构破坏。
- Future app/openapi/merchant/miniapp work，以及当前 canvas 入口扩展，必须在现有 capability 下增加或维护 `transport/{platform}`，不要创建平台前缀业务模块。
- Channel-specific verify-code TTL 已落地：email TTL 归 `mail_configs.verify_code_ttl_minutes`，SMS TTL 归 `sms_configs.verify_code_ttl_minutes`；旧 `system_settings.auth.verify_code.ttl_minutes` 已在本地 live MySQL check 中软删除。
- `admin_app` H5/LAN dev 当前仍是直连 Go backend `/api/app/v1`：runtime 默认 API base 是 `http://127.0.0.1:8080/api/app/v1`；部署或 LAN 真机调试必须用 `VITE_APP_API_BASE_URL` 覆盖为当前可访问的 Go backend origin。
- Realtime 当前是 admin WebSocket + notification fan-out + AI conversation events；AI cancel 是 REST `POST /api/admin/v1/ai-conversations/:id/messages/cancel`，WebSocket 只发 `ai.response.start/delta/completed/failed.v1`。
- Queue / scheduler 当前已有 Redis-backed scheduler lock；worker hot reload 和 DB+queue outbox 仍是 planned。
- Canvas free-generation active slice contract：`canvas_front_next` 独立 Next.js 前端只调用 `/api/canvas/v1/*`；本切片明确退休旧 AI billing 全局系统，Canvas 对话、图片、视频生成统一通过后端托管 provider 免费执行，不查余额、不扣款、不写 `ai_billing_records`。backend/frontend/root gates 和本地 live DB migration/query 已验证。
- Canvas AI runtime selector 当前按智能体场景绑定取数：`/api/canvas/v1/settings` 的 `agents.text|image|video` 来自启用的 `ai_agents` Canvas 专属场景（`canvas_text_generate` / `canvas_image_generate` / `canvas_video_generate`）；`scenes` 只是可用 Canvas 运行场景，不再是 billing metadata；Canvas AI 请求只提交 `agent_id`，provider model 由后端智能体决定；chat/video transport 对客户端提交 `model`、`provider`、`api_key`、`base_url` 直接返回 bad request，不做静默忽略。
- Canvas free-generation boundary：Canvas profile 由 `internal/module/profile/transport/canvas` 拥有 `GET/PUT /api/canvas/v1/profile`；Canvas UI 的 wallet/recharge 收费入口在旧 AI billing 删除切片中退休。payment/wallet/recharge 基础域仍保留给非本切片能力和未来会员时间制模型。
- Canvas auth/RBAC hotfix：后台菜单管理 `permissions/page-init` 默认平台字典必须包含 `admin/app/canvas`；Canvas 登录页必须读 `/api/canvas/v1/auth/login-config` 渲染 email/phone/password 登录方式，email/phone 走 `/send-code` 验证码登录并按 `allow_register` 自动开户，password 登录点击提交后再弹 slide captcha，不提供独立注册入口；`canvas_front_next` 的 `(user)` 页面由登录态 guard 保护，401 跳登录、403 显示无权限页。
- Canvas auth/RBAC transport boundary hardening：Canvas auth 由 `internal/module/auth/transport/canvas` 拥有 `/api/canvas/v1/auth/*`，Canvas current-user 由 `internal/module/user/transport/canvas` 拥有 `/api/canvas/v1/users/me`，App current-user 由 `internal/module/user/transport/app` 拥有 `/api/app/v1/users/me`；Canvas Next logout 调用 `POST /api/canvas/v1/auth/logout` 成功后才清本地 session，后端 revoke 失败时保留浏览器 session；profile transport 不拥有 current-user bootstrap，只拥有 profile read/update。旧 Canvas wallet/recharge current-user routes 不再是 Canvas AI 免费生成的 active dependency；payment/wallet transport 只保留基础充值域边界。
- Current-user bootstrap contract：admin/app/canvas 统一使用 `GET /api/{admin,app,canvas}/v1/users/me` 和同一个 DTO（`user_id`、`username`、`avatar`、`role_name`、`permissions`、`router`、`buttonCodes`）；`users/init` 不再是 bootstrap 例外；已删除废弃首页捷径功能、admin 前端入口、后端保存路由和本地 live MySQL 表。Canvas SQL seed 包含 PAGE rows（`canvas_page`、`canvas_image_page`、`canvas_video_page`、`canvas_prompts_page`、`canvas_assets_page` at `/assets`、`canvas_profile_page`）以及 BUTTON rows（`canvas_access`、`canvas_prompt_read`、`canvas_asset_read`、`canvas_ai_image_generate`、`canvas_ai_video_generate`）；`canvas_ai_text_generate` 是 live MySQL 软删除孤儿行，不是 active BUTTON code；`/asset-library` 是已删除死页，不是 active Canvas route；top/mobile navigation 和 route guard 使用 backend `router`，按钮/动作 `can(code)` 只读 `buttonCodes`。
- Admin Vue runtime adapter contract：`admin_front_ts` bootstrap 后调用 `setupDynamicRoutes()`，通过 `UsersApi.me()` 拉 `GET /api/admin/v1/users/me`，将 `permissions/router/buttonCodes` 写入 Pinia；动态路由由 backend `router` + local view registry 生成，按钮显隐只读 `userStore.can(code)`；用户管理启停调用 `PATCH /api/admin/v1/users/:id/status`，AI 智能体测试调用 `POST /api/admin/v1/ai-agents/:id/test`，都不走批量编辑、provider test 或浏览器侧 provider credential 兜底。
- Codex knowledge base first slice 已落地：`docs/knowledge/*` 组织当前 runtime、agent 操作模型、MySQL schema 入口、DB schema ownership map、full-stack module map、backend capability manifest、Admin Vue source-quality inventory、Admin direct external helper review、Admin dev test download source-quality review、Admin useValidator source-quality review、Admin upload demo source-quality review、Admin demo any source-quality review、Admin AI image payload source-quality review、Canvas AI request contract review、Canvas RBAC permission contract review、Canvas asset route contract review、Canvas auth logout contract review、Admin user status contract review 和 Admin AI agent test contract review；表结构引用必须优先使用 live MySQL snapshot `docs/db/mysql-live-schema-2026-06-07.md` / `docs/db/mysql-live-schema-2026-06-07.sql`，表到 Go source owner 的映射看 `docs/knowledge/db-schema-ownership-map-2026-06-07.md`，跨后端路由/前端 API/live DB ownership 的能力地图看 `docs/knowledge/full-stack-module-map-2026-06-07.md`，Go backend capability 源码归属看 `docs/knowledge/backend-capability-manifest-2026-06-07.md`，Admin Vue `any/as any/fallback/direct external HTTP` 债务库存看 `docs/knowledge/admin-front-source-quality-inventory-2026-06-07.md`，Admin direct external helper 删除口径看 `docs/knowledge/admin-front-direct-external-helper-review-2026-06-07.md`，Admin Header breadcrumb source-quality 口径看 `docs/knowledge/admin-front-header-breadcrumb-source-quality-review-2026-06-07.md`，Admin forgot-password request-error source-quality 口径看 `docs/knowledge/admin-front-forgot-password-error-source-quality-review-2026-06-07.md`，Admin JsonEditor source-quality 口径看 `docs/knowledge/admin-front-json-editor-source-quality-review-2026-06-07.md`，Admin DIcon source-quality 口径看 `docs/knowledge/admin-front-dicon-source-quality-review-2026-06-07.md`，Admin wangEditor source-quality 口径看 `docs/knowledge/admin-front-editor-source-quality-review-2026-06-07.md`，Admin DownloadManager source-quality 口径看 `docs/knowledge/admin-front-download-manager-source-quality-review-2026-06-07.md`，Admin dev test 下载页 source-quality 口径看 `docs/knowledge/admin-front-dev-test-download-source-quality-review-2026-06-07.md`，Admin useValidator source-quality 口径看 `docs/knowledge/admin-front-validator-source-quality-review-2026-06-07.md`，Admin upload demo source-quality 口径看 `docs/knowledge/admin-front-upload-demo-source-quality-review-2026-06-07.md`，Admin demo any 清零口径看 `docs/knowledge/admin-front-demo-any-source-quality-review-2026-06-07.md`，Admin AI image payload source-quality 口径看 `docs/knowledge/admin-front-ai-image-payload-source-quality-review-2026-06-07.md`，Canvas AI `agent_id` 请求契约看 `docs/knowledge/canvas-ai-request-contract-review-2026-06-07.md`，Canvas RBAC 权限码口径看 `docs/knowledge/canvas-rbac-permission-contract-review-2026-06-07.md`，Canvas 资产页路由口径看 `docs/knowledge/canvas-asset-route-contract-review-2026-06-07.md`，Canvas logout 契约口径看 `docs/knowledge/canvas-auth-logout-contract-review-2026-06-07.md`，Admin 用户状态契约口径看 `docs/knowledge/admin-user-status-contract-review-2026-06-07.md`，Admin AI 智能体测试契约口径看 `docs/knowledge/admin-ai-agent-test-contract-review-2026-06-07.md`。live schema snapshot 由 `scripts/export-live-mysql-schema.ps1` 从 `admin_back_go/.env` 的 `MYSQL_DSN` 查验生成；ownership map 由 `scripts/export-db-schema-ownership-map.ps1` 从 latest schema artifact + current Go source 生成；module map 由 `scripts/export-full-stack-module-map.ps1` 从 generated source artifacts 合成；capability manifest 由 `scripts/export-backend-capability-manifest.ps1` 从 current Go source + route/schema ownership artifacts 生成；Admin Vue source-quality inventory 由 `scripts/export-admin-front-source-quality-inventory.ps1` 从 current `admin_front_ts/src` 生成；后续质量递进路线由 `docs/architecture/09-codex-first-quality-runway.md` 统一约束，不把 remaining fallback 写成 completed。

## Current verification gaps

- 2026-05-30 after re-entering COS upload driver secrets for the current `APP_SECRET`, full admin smoke passed and `UPLOAD-RUNTIME-001` is closed as a runtime data repair, not a code change. The summary reported `upload_token_probe=passed`, `upload_token_code=0`, and `upload_token_provider=cos`.
- 2026-05-30 Docker-first backend readiness was restored on `127.0.0.1:8080`; `/health` and `/ready` passed with database/redis/token_redis/queue_redis/realtime all `up`.
- 2026-05-27 multi-platform Phase 2 code/docs/frontend gates and the later full admin smoke gate have now passed for the current local Go/Vue runtime.
- Docker-first readiness 和 smoke 是两条验证链：Docker runtime 用 `127.0.0.1:8080 /health /ready`；smoke 脚本默认临时端口是 basic `127.0.0.1:18080`、full `127.0.0.1:18081`。
- `admin_app` 机器局域网默认值已清掉；本轮未跑真机 smoke，LAN 调试仍需按本机 IP 配置 `VITE_APP_API_BASE_URL`、后端监听地址、防火墙和 `CORS_ALLOW_ORIGINS`。
- Export runtime V2 code/tests are complete in the export worktree, but the credential-gated real submit-to-COS smoke has not been run in this environment; do not claim COS upload runtime closure until `scripts/export-task-smoke.ps1 -RunRealExport` passes。
- Canvas Front Next live DB baseline 已应用/查询 `20260531_canvas_front_next_integration.sql`：当前已验证的 canvas 业务表仍是 `canvas_prompts`、`canvas_assets`；本旧 AI billing 删除切片已新增并在本地 live DB 验证 `canvas_video_tasks` 保存视频异步任务事实。`canvas_users` / `canvas_credit_logs` / `canvas_settings` / `canvas_projects` / `canvas_wallets` 不存在；full-admin-smoke、后端全量 Go tests、Next test/typecheck/build 和 root governance gates 已通过。

## Latest status / change-log pointers

详细变更记录看 `docs/status/archive/2026-05-runtime-change-log.md`。近期关键批次：

```text
2026-06-07 Admin Vue upload demo source-quality cleanup verified: `src/views/Main/component/upload/index.vue` now imports `UploadMediaItem` and uses `ref<UploadMediaItem[]>([])`, while `UpMediaList.vue` shares the same model through `components/media.ts`; `tests/shared/upload/upload-demo-source-quality.test.ts` guards the slice, and source-quality inventory now reports 280 source files, 7 `any` candidates, 0 `catch(...: any)` candidates, 562 fallback candidates, and 0 direct external HTTP candidates; this closes only upload demo media-list any debt
2026-06-07 Admin Vue useValidator source-quality cleanup verified: `src/hooks/web/useValidator.ts` now types validator values as `ValidatorValue = string`, keeps `LengthRange` explicit, and uses `resolveValidatorMessage(message, fallback)` instead of `message || fallback`; `tests/shared/validator/use-validator-source-quality.test.ts` guards the slice, and source-quality inventory now reports 7 `any` candidates, 0 `catch(...: any)` candidates, 562 fallback candidates, and 0 direct external HTTP candidates; this closes only useValidator validator input/message fallback debt
2026-06-07 Admin AI agent test owner decision closed: Admin Vue now calls `POST /api/admin/v1/ai-agents/:id/test` through `AiAgentApi.test`, the AI agent list exposes the test action only under `ai_agent_test` for enabled rows, `tests/shared/ai/ai-agent-api.test.ts` guards the API/page wiring, generated API drift artifacts now report 258 exact frontend backend calls, 19 backend source-only rows, and 0 owner-decision-required routes; this closes API-DRIFT-001
2026-06-07 Admin user status owner decision closed: Admin Vue now calls `PATCH /api/admin/v1/users/:id/status` through `UsersListApi.changeStatus`, the user list page exposes enabled/disabled status actions under `user_userManager_edit`, `tests/shared/user/user-list.test.ts` guards the API/page wiring; current generated API drift artifacts now report 258 exact frontend backend calls, 19 backend source-only rows, and 0 owner-decision-required routes after the later Admin AI agent test closure; this closes only the Admin users/:id/status row of API-DRIFT-001
2026-06-07 Canvas auth logout owner decision closed: Canvas Next now calls `POST /api/canvas/v1/auth/logout` through `src/services/api/auth.ts`, `useUserStore.logout()` revokes backend session before local cleanup and preserves local state on backend failure, `tests/shared/canvas-auth-boundary.test.ts` plus store/API tests guard the behavior; current generated API drift artifacts now report 258 exact frontend backend calls, 19 backend source-only rows, and 0 owner-decision-required routes after the later Admin AI agent test closure; this closes only the Canvas auth/logout row of API-DRIFT-001
2026-06-07 Canvas asset route ambiguity closed: live MySQL and seed/docs expose `canvas_assets_page` only at `/assets`, `canvas_front_next/src/app/(user)/asset-library/page.tsx` was removed as an unreachable dead page, `tests/shared/canvas-rbac-shell.test.ts` guards against reintroducing it, generated runtime/frontend API inventories were refreshed, and `docs/knowledge/canvas-asset-route-contract-review-2026-06-07.md` records the evidence; this closes CANVAS-DOC-002 only
2026-06-07 Canvas RBAC text-generation permission drift closed: live MySQL shows `canvas_ai_text_generate` as a soft-deleted orphan row, `canvas_front_next/src/features/rbac/canvas-permissions.ts` no longer defines it, `tests/shared/canvas-rbac-shell.test.ts` guards against reintroducing it, and `docs/knowledge/canvas-rbac-permission-contract-review-2026-06-07.md` records the source/live DB evidence; this closes CANVAS-DOC-001 only
2026-06-07 Canvas AI request contract guard verified: chat/video Canvas transports no longer accept client `model` request fields, reject `model/provider/api_key/base_url` before service invocation, keep service `ModelID` empty at the HTTP boundary, and `docs/knowledge/canvas-ai-request-contract-review-2026-06-07.md` records the active `agent_id` request shapes; this closes CANVAS-DOC-003 only
2026-06-07 admin_front_ts SearchDialog route-walk type guard verified: `tests/layout/search-dialog-source-quality.test.ts` guards `src/views/Layout/components/Header/components/SearchDialog.vue` against `any/as any`, the component now walks `PermissionMenuItem` data through explicit `SearchMenuNode` / `SearchResultItem` types, targeted Vitest and `vue-tsc` passed; this closes ADMIN-FRONT-HARDENING-002 only
2026-06-07 Admin Vue Header breadcrumb source-quality cleanup verified: `src/views/Layout/components/Header/index.vue` now derives breadcrumbs through typed `PermissionMenuItem` route-walk, no longer uses `any[]`, `item: any`, or `getPath(...) || []`, `tests/layout/header-source-quality.test.ts` guards the slice, and source-quality inventory now reports Header/index.vue with no configured finding; this closes only the Header breadcrumb debt
2026-06-07 Admin Vue forgot-password request-error source-quality cleanup verified: `src/views/Login/composables/useForgotPassword.ts` now catches `unknown`, displays only `requireRequestErrorMessage(...)`, and fails closed on non-Error / empty `Error.message` instead of `error?.message || sendFailed/resetFailed`; `tests/shared/user/forgot-password-source-quality.test.ts` guards source and empty-message behavior, and `docs/knowledge/admin-front-forgot-password-error-source-quality-review-2026-06-07.md` records the decision; this closes only the forgot-password request-error fallback debt
2026-06-07 Admin Vue wangEditor source-quality cleanup verified: `src/views/Main/component/display/components/Editor.vue` now imports typed `Boot`/`IDomEditor`/`IEditorConfig`/`IModuleConf`, keeps `editorRef` and handlers typed, and rejects empty upload URLs through `requireUploadURL(result.url)` instead of `result.url || ''`; `tests/shared/editor/editor-source-quality.test.ts` guards the slice; this closes only the wangEditor wrapper any/as-any/upload URL fallback debt
2026-06-07 Admin Vue DownloadManager source-quality cleanup verified: `src/components/DownloadManager/src/download.ts` now catches download errors as `unknown`, classifies user cancellation through `isDownloadUserCancelled(...)`, requires real non-empty `Error.message` values through `requireDownloadError(...)`, rethrows Web fetch failures instead of hiding them behind `window.open(url, '_blank')`, and derives filenames through explicit helpers instead of logical-or fallback chains; `tests/shared/download-manager/download-manager-source-quality.test.ts` guards the slice, and source-quality inventory now reports 7 `any` candidates, 0 `catch(...: any)` candidates, 562 fallback candidates, and 0 direct external HTTP candidates; this closes only DownloadManager download.ts catch-any / failed-download silent fallback / filename fallback debt
2026-06-07 Admin Vue dev test download source-quality cleanup verified: `src/views/Main/test/index.vue` now catches download errors as `unknown`, requires real non-empty `Error.message` values through `requireDevTestDownloadErrorMessage(...)`, and uses `optionalDownloadFilename(...)` instead of `testFilename.value || undefined`; `tests/shared/download-manager/dev-test-download-source-quality.test.ts` guards the slice, and `docs/knowledge/admin-front-dev-test-download-source-quality-review-2026-06-07.md` records the evidence; this closes only the dev test page download error-handling debt
2026-06-07 Admin Vue DIcon source-quality cleanup verified: `src/components/DIcon/src/index.vue` now types the dynamic Element Plus icon module as `typeof import('@element-plus/icons-vue')` and narrows runtime names through a `keyof` guard instead of `(mod as any)`, `tests/shared/icon/dicon-source-quality.test.ts` guards the slice; this closes only the DIcon dynamic-module any/as-any debt
2026-06-07 Admin Vue JsonEditor source-quality cleanup verified: `src/components/JsonEditor/src/json.ts` now owns explicit empty-editor `{}` parsing, formatting, and non-empty parse-error message requirements; `src/components/JsonEditor/src/index.vue` catches `unknown`, uses helper functions and `jsonEditor.*` i18n keys, and `tests/shared/json-editor/json-editor-source-quality.test.ts` plus `tests/shared/i18n/no-visible-chinese.test.ts` guard the slice; this closes only JsonEditor parse-error/empty-editor/i18n debt
2026-06-07 Admin Vue direct external helper cleanup verified: `admin_front_ts/src/api/tools.ts` was unused and deleted, `tests/shared/api/no-direct-external-helper.test.ts` guards against reintroducing the retired `api.btstu.cn` random-image helper, generated inventories now report 274 frontend API call expressions, 3 external HTTP calls, and 0 Admin Vue direct external HTTP candidates; this closes ADMIN-FRONT-HARDENING-003 only
2026-06-07 Admin Vue source-quality inventory refreshed after upload demo cleanup: `scripts/export-admin-front-source-quality-inventory.ps1` generated `docs/knowledge/admin-front-source-quality-inventory-2026-06-07.md`, scanning 280 current Admin Vue source files after stripping comments; current inventory records 0 `any` candidates, 0 `as any` candidates, 0 `Record<string, any>` candidates, 0 `catch(...: any)` candidates, 555 fallback candidates, and 0 direct external HTTP candidates after deleting the unused random-image helper and closing forgot-password, JsonEditor, DIcon dynamic-module, wangEditor wrapper, DownloadManager download error/filename debt, dev test download error debt, useValidator validator typing/message debt, upload demo media-list typing debt, and demo any typing debt; this is review evidence only and does not close the remaining general source-quality debt
2026-06-07 admin_front_ts TableActions i18n guard verified: shared TableActions refresh text now uses `common.actions.refresh`, `tests/shared/i18n/no-visible-chinese.test.ts` includes `src/components/Table/src/components/TableActions.vue`, and targeted Vitest passed; this closes ADMIN-FRONT-HARDENING-001 only
2026-06-07 backend capability manifest added: `scripts/export-backend-capability-manifest.ps1` generated `docs/knowledge/backend-capability-manifest-2026-06-07.md`, mapping Go capabilities to source dirs, direct service/repository/model files, route surfaces, direct tests, and live DB model-owned tables; current result is 34 capabilities, 280 route registrations covered, and 3 helper packages not promoted (`auth/verifycode`, `payment/serialno`, `queuemonitor/asynqmonui`)
2026-06-07 full-stack module map added/refreshed: `scripts/export-full-stack-module-map.ps1` generated `docs/knowledge/full-stack-module-map-2026-06-07.md`, joining backend route inventory, frontend API inventory, DB schema ownership, and source-only route review by capability; current result is 280 backend route registrations joined, 258 frontend exact backend calls assigned, 0 unassigned exact frontend calls, 56 live DB tables mapped, and 0 owner-decision-required routes preserved
2026-06-07 DB schema ownership map added: `scripts/export-db-schema-ownership-map.ps1` generated `docs/knowledge/db-schema-ownership-map-2026-06-07.md` from the latest live MySQL schema artifact and current Go source; current result is 56 live tables reviewed, 55 go-model tables, and 1 live-schema-only table (`canvas_prompts_backup_20260601_before_infinite_import`)
2026-06-07 live MySQL schema snapshot refreshed: `docs/db/mysql-live-schema-2026-06-07.md` / `.sql` generated from live `admin` database via information_schema, per-table COUNT(*), and mysqldump --no-data; base table count remains 56, and `scripts/check-runtime-doc-facts.ps1` now discovers the latest tracked schema artifact instead of hard-coding the previous date
2026-06-07 API source-only route review added/refreshed: `scripts/export-api-source-only-route-review.ps1` generated `docs/knowledge/api-source-only-route-review-2026-06-07.md`, classifying the 19 backend admin/canvas source-only rows into 4 runtime/system, 3 queue monitor, 6 retained Canvas payment/wallet, 6 uploadConfig parametric helper covered, and 0 owner-decision-required routes
2026-06-07 frontend/backend API drift report added/refreshed: `scripts/export-frontend-backend-api-drift.ps1` generated `docs/knowledge/frontend-backend-api-drift-2026-06-07.md`; current exact frontend backend calls are 258/258 route-match, 0 method-mismatch, and 0 no-backend-route, with 19 backend admin/canvas source-only rows kept as review evidence
2026-06-07 frontend API source inventory added/refreshed: `scripts/export-frontend-api-inventory.ps1` generated `docs/knowledge/frontend-api-inventory-2026-06-07.md` with 274 frontend API call expressions, 239 exact Admin backend calls, 19 exact Canvas backend calls, 2 parametric Admin helper calls, 3 external HTTP calls, 0 backend /api calls outside known prefixes, and 0 unresolved expressions
2026-06-07 backend route source/contract inventory added: `scripts/export-backend-route-inventory.ps1` generated `docs/knowledge/backend-route-inventory-2026-06-07.md` with 280 route registrations, 0 unresolved paths, 1 callback exception, and 0 unmatched route_meta keys; `scripts/export-backend-route-contract-drift.ps1` generated `docs/knowledge/backend-route-contract-drift-2026-06-07.md` with 280 contract-exact, 0 contract-prefix-only, 0 source-docs-only, and 0 undocumented-exact rows
2026-06-07 runtime inventory snapshot added: `scripts/export-runtime-inventory.ps1` generates `docs/knowledge/runtime-inventory-2026-06-07.md` from current Go/Vue/Canvas source manifests, backend module transport dirs, backend route fragments, Admin Vue source directories, Canvas pages/services/stores, and latest MySQL schema artifact
2026-06-06 runtime documentation fact checker added: `scripts/check-runtime-doc-facts.ps1` verifies selected go.mod/package.json versions, admin/canvas current-user route facts, stale users/init/dict/request/job phrases, and tracked MySQL schema artifacts; default mode is non-live, `-LiveSchema` re-exports from MySQL and compares table count
2026-06-06 Codex-first knowledge base / live MySQL schema snapshot first slice: docs/knowledge entry, runtime knowledge, agent operating model, live schema markdown+DDL artifact, and repeatable export script added; live MySQL `admin` has 56 base tables verified via information_schema/COUNT(*)/mysqldump --no-data
2026-06-06 admin_front_ts shared primitive quality hardening verified: Search/AppTable/RemoteSelect shared typings no longer rely on guarded any/as any/Record<string, any> or contract-hiding option/form/page fallbacks; ColumnSetting/Search visible labels use i18n keys; targeted Vitest, vue-tsc, diff checks, and root governance passed for this frontend-only slice
2026-06-04 Canvas AI video transport ownership: `/api/canvas/v1/ai/videos*` external URLs unchanged, runtime/model/repository ownership moved from `internal/module/canvas` to `internal/module/ai/video`, and `canvas_video_tasks` physical table name remains unchanged
2026-06-01 Canvas free generation / old AI billing retirement active slice: /api/canvas/v1/settings exposes agents.text/image/video from `canvas_text_generate` / `canvas_image_generate` / `canvas_video_generate`, Canvas generation is free, old `/api/admin/v1/ai-billing-rules*` and `ai_billing_*` active contract are retired, and payment/wallet基础域只保留给非本切片能力
2026-06-01 backend fallback hardening: AI provider canonical engine_type and stored-status fail-closed behavior, upload config non-COS read fail-closed behavior, and mail/SMS runtime VerifyCodeTTL missing-config fail-closed behavior verified with backend full tests
2026-05-31 Canvas Front Next integration baseline verified: canvas auth platform/routes, public prompts/assets/settings, backend-managed text/image/video AI generation, Next frontend boundary/type/build gates, live DB migration/query, full-admin-smoke, and root governance gates passed; the temporary wallet/recharge thin routes from that baseline are retired by the 2026-06-01 free-generation slice
2026-06-01 current-user users/me contract cleanup: admin/app/canvas users/me share one DTO, users/init removed, obsolete shortcut feature removed from backend/frontend/live DB, user transport owns app/canvas current-user routes, Canvas PAGE/BUTTON RBAC live DB checker passed
2026-05-31 Canvas auth/RBAC route-line hardening verified: canvas auth/users URL prefixes are owned by matching transport/canvas packages instead of app/admin dynamic registration; backend architecture/targeted/full tests, go vet, basic-admin-smoke, full-admin-smoke, and root governance gates passed. Old Canvas wallet/recharge prefixes are no longer active Canvas free-generation dependencies
2026-05-31 RESTful API naming full standardization: frontend CRUD wrappers and active call sites use standard names only; backend page dictionary routes bind PageInit-style handlers/services; current-user bootstrap is `users/me`
2026-05-31 payment serial readability hardening: RCG/PAY/WLT no longer append a 20-digit zero-padded sequence; new rows use compact uppercase base36 suffix while old rows stay unchanged
2026-05-31 export runtime v2 verified: registry-driven export runtime, `kind/platform/object_key`, frontend export submit helper, migration dry-run, and gated real submit-to-COS smoke all passed
2026-05-31 payment/wallet final review: legacy wallet consume DTO/service/repository/i18n surfaces removed; wallet mutations are now explicit Debit/Credit with ai_generate/ai_refund sources
2026-05-31 payment/wallet/AI billing redesign verified: backend full tests, frontend targeted tests/typecheck/quality, live DB migration check, and full-admin-smoke passed
2026-05-30 COS upload secrets re-entered and full-admin-smoke passed; UPLOAD-RUNTIME-001 closed
2026-05-30 AI chat cancel late-event guard fixed: canceled request ids survive later completions and WebSocket start/delta/completed/failed acknowledgements only mutate the matching in-flight request or matching streaming assistant message
2026-05-30 payment finalizer state regression hardening: stale finalizer snapshots cannot downgrade credited recharges back to paid or credit recharges that were closed concurrently; recharge paid markers are CAS guarded
2026-05-30 payment race/return hardening: CAS misses no longer credit or leak stale pay_url, wallet first-create duplicate races return the existing wallet, callback audit amount parsing rejects signed cent fragments, and recharge auto-sync/return-url sync keep retrying while Alipay still says paying
2026-05-30 payment hardening follow-up: linked recharge close, paid-uncredited compensation, Alipay amount parsing, recharge add permission UI
2026-05-30 payment frontend/backend hardening: transient recharge sync failures stay retryable, payment-order sync is shown only for `paying`, disabled/deleted bound configs can still settle existing orders, open-order configs are locked, payment state updates use CAS, and callback audit JSON no longer blocks settlement
2026-05-30 production websocket/domain and docker-first deploy docs alignment
2026-05-30 wallet transaction_no hardening fixed: serial no-wrap + transaction_no duplicate retry for consume and recharge credit
2026-05-29 multi-platform backend boundary Phase 2 gates passed; final smoke pending
2026-05-29 transport admin alias cleanup
2026-05-29 wallet payment aggregation
2026-05-28 infra runtime layer rename
2026-05-28 final transport boundary guard
2026-05-28 admin transport shell waves
2026-05-28 AI/small-module/shared aggregation waves
2026-05-27 multi-platform architecture direction snapshot
```

## Current module details

详细 per-module 状态看：

```text
docs/status/module-matrix.md
```

## Current known issues

当前未闭环 bug / WIP 看：

```text
docs/status/known-issues.md
```

## Current verified RBAC loop

```text
login -> AuthToken -> users/me
permission create DIR/PAGE/BUTTON
role update grants PAGE/BUTTON
users/me returns router + BUTTON-only buttonCodes
role restore
permission subtree delete
users page-init + users list
logout
```

Verification command:

```powershell
cd E:\admin_go\admin_back_go
powershell -ExecutionPolicy Bypass -File .\scripts\basic-admin-smoke.ps1 -Account 15671628271 -Password 123456
powershell -ExecutionPolicy Bypass -File .\scripts\full-admin-smoke.ps1 -Account 15671628271 -Password 123456
```
