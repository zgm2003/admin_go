# Admin Go Migration Current Status

状态更新时间：2026-05-09

本文只记录已经验证的 Go/Vue 迁移事实。不要把 planned 写成 implemented。

| Module | Go backend status | Frontend status | Tests | Smoke | Docs | Remaining risk |
| --- | --- | --- | --- | --- | --- | --- |
| health / ready | implemented: liveness separated from DB/Redis readiness, checks include database/redis/token_redis/queue_redis/realtime | n/a | `internal/readiness`, `internal/module/system`, `internal/bootstrap` | `/ready` in basic smoke | architecture + contract + deployment docs | readiness does not replace full smoke |
| auth login/session | implemented | adapted | `internal/module/auth`, `internal/module/session` | password/code login, refresh/logout, login log count | architecture + contract | real SMS/email sender still dev-mode |
| captcha | implemented | adapted with `go-captcha-vue` | `internal/module/captcha` | real slide challenge in smoke | architecture + contract | only slide supported |
| auth platform | implemented: login types, captcha type, token TTL and session policy are DB-managed facts | adapted | `internal/module/authplatform` | init/list in smoke | `docs/contracts/admin-api-v1.md` | broader policy UI polish later |
| RBAC bootstrap | implemented | adapted | `internal/module/user`, `internal/module/permission`, `internal/bootstrap` | users/me + users/init | architecture + contract | no multi-role model in phase one |
| permission definitions | implemented | adapted | `internal/module/permission` | create DIR/PAGE/BUTTON + batch delete | architecture + contract | operation-log hardening is next phase |
| roles | implemented | adapted | `internal/module/role` | grant/restore test role in smoke | architecture + contract | deleting bound roles intentionally blocked |
| users management | implemented for page-init/list/edit/batch-edit/status/delete/export submit; export submit creates `export_tasks` pending row and enqueues `export:run:v1` low queue | adapted for user manager page; export button uses Go REST `POST /api/admin/v1/users/export` and keeps `user_userManager_export` | `internal/module/user`, `internal/module/exporttask`, `internal/jobs`, `internal/server`, `internal/bootstrap`; frontend user/export-task API Vitest | users page-init + list in basic smoke; export task read-only probes in full smoke matrix | architecture + contract | worker requires enabled queue and COS config for actual file generation |
| user legacy closure | implemented: quick-entry save `PUT /api/admin/v1/users/me/quick-entries`, login-log read `GET /api/admin/v1/users/login-logs/page-init` + `GET /api/admin/v1/users/login-logs`, and user-session revoke `PATCH /api/admin/v1/user-sessions/:id/revoke` + `PATCH /api/admin/v1/user-sessions/revoke`; session list still derives active/expired/revoked from `revoked_at` + `refresh_expires_at` and never returns token hash fields | adapted: home quick-entry, login-log page, and session kick/batchKick now use Go REST; `forgetPassword` remains the deliberate legacy account-security slice; dead `EditPassword` frontend definition removed | `internal/module/session`, `internal/module/userquickentry`, `internal/module/userloginlog`, `internal/module/usersession`, `internal/server`, `internal/bootstrap`; frontend `src/api/user/*`, `src/types/user.ts`, user API Vitest | full smoke probes quick-entry save/restore, login-log page-init/list, user-session page-init/list/stats token-hash non-leak, and current-session anti-kick | contract + smoke matrix + user legacy closure spec/plan + backend architecture | actual non-current kick smoke is intentionally not run against random live sessions; Redis cleanup is covered by focused Go tests and current-session anti-kick by smoke |
| export tasks | implemented: `GET /api/admin/v1/export-tasks/status-count`, `GET /api/admin/v1/export-tasks`, `DELETE /api/admin/v1/export-tasks/:id`, `DELETE /api/admin/v1/export-tasks`; worker handles `kind=user_list`, writes xlsx with string cells, uploads to current COS, marks success/failed and sends notification | adapted: `src/api/system/exportTask.ts` uses Go REST `request`, no legacy export-task adapter | `internal/module/exporttask`, `internal/jobs`, `internal/bootstrap`, `internal/server`; frontend `tests/shared/system/export-task-api.test.ts` | full smoke read-only probes status-count/list; actual export e2e requires queue worker + COS | contract + backend architecture + smoke matrix | no universal export platform; no OSS runtime; no retry/cancel/COS delete UI in this slice |
| profile / account security / avatar upload | implemented: `GET /api/admin/v1/profile`, `GET /api/admin/v1/users/:id/profile`, `PUT /api/admin/v1/profile`, `PUT /api/admin/v1/profile/security/password`, `PUT /api/admin/v1/profile/security/email`, `PUT /api/admin/v1/profile/security/phone`; self-update and security writes record operation logs, no user-manager button permission | adapted: personal base info/security and home profile summary read/write Go profile contract; avatar upload uses shared Go upload token client via `UpMedia` | `internal/module/user`, `internal/bootstrap`, `internal/server`; frontend `vue-tsc`; targeted Security eslint exits 0 with style warnings only | full smoke probes read/update, profile operation log, and non-mutating account-security failure cases | contract + profile/avatar spec/plan + account-security spec/plan + smoke matrix | `auth/send-code` is still public dev-mode sender; no avatar crop/server-side upload |
| system logs | implemented baseline: slog stdout + optional lumberjack file output, process-specific api/worker log files, read-only logstore, REST files/lines API with path traversal guard | adapted to Go REST API | `internal/platform/logstore`, `internal/platform/logging`, `internal/config`, `internal/module/systemlog`, `internal/server` | full smoke probes init/files shape and conditionally probes lines when a log file exists | architecture + contract + plan + smoke matrix | first phase is read-only; no ELK/Loki, no delete/clear/download |
| operation log | implemented: explicit route metadata plus request/response JSON capture with sanitization and 64KB cap | adapted: list summary reads real request payload / response data and detail dialog formats JSON | `internal/module/operationlog`, `internal/middleware`, `internal/server`; frontend payload formatter Vitest | full smoke covers init/list/create-triggered log/delete | architecture + contract + smoke matrix | old historical rows may not have captured payload before middleware hardening |
| notifications current-user read/list | implemented: REST init/list/unread-count/mark-read/delete scoped by token user + platform/all + is_del=2; no RBAC button rule or operation-log metadata for current-user read/delete | adapted: notification center and home/runtime notification calls use Go REST and versioned `notification.created.v1` listener | `internal/module/notification`, `internal/server`, `internal/bootstrap`; frontend targeted eslint | full smoke read-only probes init/list/unread-count | contract + notification spec/plan + smoke matrix | notification.created.v1 Redis/WebSocket fan-out now implemented through notification task dispatch |
| notification task publish / scheduler | implemented: REST init/status-count/list/create/cancel/delete, enum/dict/validate, route permission + operation log metadata, `notification:dispatch-due:v1` + `notification:send-task:v1`; dispatch-due now compensates both immediate `send_at IS NULL` pending tasks and due scheduled tasks; schedule registration is owned by `cron_task.name=notification_task_scheduler` Go registry in `admin-worker` | adapted: publish page uses Go REST typed client, no legacyRequest; NotificationRuntime shows every `notification.created.v1` instead of filtering normal notices | `internal/module/notificationtask`, `internal/jobs`, `internal/server`, `internal/bootstrap`; frontend vue-tsc + targeted eslint | full smoke read-only probes init/status-count/list; manual e2e verified task create -> worker send -> WebSocket `notification.created.v1` | contract + notification task spec/plan + smoke matrix + backend architecture | DB+queue is not transactional; outbox planned if stronger consistency is needed; Redis Pub/Sub realtime fan-out implemented for admin notification.created.v1 |
| admin chat room | removed: admin chat is outside current admin scope; Go `internal/module/chat` and `/api/admin/v1/chat...` routes are deleted; DB migration `admin_back_go/database/migrations/20260507_remove_admin_chat.sql` removes `chat_*` tables and chat menu/role grants | removed: `admin_front_ts/src/views/Main/chat`, `src/api/chat`, `src/store/chat.ts`, `tests/shared/chat`, and `menu.chat` i18n are deleted; AI chat stays under `src/views/Main/ai/chat` + `src/api/ai/chat.ts` | route/module deletion plus residue scans; normal backend/frontend build gates | no smoke; no active admin chat path remains | admin API/realtime contracts + deletion migration | destructive migration drops chat history tables by product decision; do not confuse with AI chat |
| AI provider config / OpenAI first slice | implemented for the first AI menu only: `admin_back_go/database/migrations/20260509_ai_openai_provider_config.sql` keeps `ai_providers` as the compatibility provider table, adds `ai_provider_models`, allows empty `base_url` for the official OpenAI default, records model sync status/error fields, and fixes AI menu sort to providers/apps/knowledge/tools/runs/chat. Active provider-config backend is `internal/module/aiprovider` plus `internal/platform/ai/provider`; first driver is exactly `openai`; API key is encrypted server-side and never returned. Other AI modules (`aiapp`, `aichat`, `aiknowledgemap`, `aitoolmap`, `airun`) remain existing surfaces but are not part of this completed slice. Provider config no longer has a default-model concept; `20260509_remove_ai_provider_default_model.sql` removes the old provider-model default marker column, and future 智能体配置 owns concrete model selection. `20260509_cleanup_ai_provider_models_snapshot.sql` makes `ai_provider_models` a current selectable model snapshot only and removes `raw_json` / `source` / soft-delete history / fake row auditors; it also removes provider-level `config_json` and fake auditors from `ai_providers`. | adapted for provider config page: AI product menu order is `/ai/providers`, `/ai/apps`, `/ai/knowledge`, `/ai/tools`, `/ai/runs`, `/ai/chat`; `src/api/ai/providers.ts` exposes `openai` only, create/edit preview, saved-key edit preview, sync/model-list/update-model APIs, `model_ids/model_display_names`, and no secret/raw/source/config JSON response fields. | `internal/platform/ai/provider`, `internal/module/aiprovider`, route/meta/router tests; frontend AI provider API Vitest + `npm run build:check` | basic/full smoke users/init gate requires providers/apps/knowledge/tools/runs/chat order and rejects retired goods/cine/model/agent/prompt entries; full smoke provider read gate requires driver `openai`, health/model-sync statuses `unknown/ok/failed`, and no plaintext/encrypted key/raw/source/config leaks | OpenAI provider config spec/plan + admin API contract + smoke matrix | live OpenAI `/models` and chat E2E are credential-gated; next AI slices must separately finish 智能体配置、知识库、工具管理、运行监控、AI对话 |
| system cron tasks | implemented: REST `cron_task` CRUD/logs + DB-backed worker scheduler registration + registry_status; `notification_task_scheduler` maps to `notification:dispatch-due:v1`, `ai_run_timeout` maps to `ai:run-timeout:v1`, and old PHP handler strings are legacy provenance only for missing rows | adapted: cron task page uses Go REST, no `legacyRequest`, and displays Go task type instead of PHP class for registered tasks | `internal/module/crontask`, `internal/jobs`, `internal/bootstrap`, `internal/server`; frontend `tests/shared/system/cron-task-api.test.ts` | full smoke probes init/list/logs and registry status including `ai_run_timeout` | admin API contract + backend architecture + smoke matrix | registered payment cron is now `payment:close-expired-order:v1` and `payment:sync-pending-order:v1`; old pay reconcile/refund/fulfillment cron rows are retired until a separate spec exists |
| client version management | implemented: backend module `internal/module/clientversion`, enum/dict/validate, REST `/api/admin/v1/client-versions`, public `current-check`, COS manifest publisher, route permission + OperationLog metadata | adapted: version page and TauriManager use `src/api/system/clientVersion.ts` with Go REST `request`, no legacyRequest | `internal/enum`, `internal/dict`, `internal/validate`, `internal/platform/storage/cos`, `internal/module/clientversion`, `internal/bootstrap`, `internal/server` | full smoke probes page-init/list/update-json read-only | client version spec/plan + admin API contract | server-side manifest publish currently supports COS only; DB table is `client_versions` and button permission codes are `system_clientVersion_*`; old Tauri names only remain in migration source conditions or legacy PHP references |
| queue / worker / monitor | implemented baseline: producer/consumer wrapper, handler registry, DB-backed cron-to-queue boundary via system cron tasks, read-only Asynq inspector summary, official `asynqmon` mounted at `/api/admin/v1/queue-monitor-ui/*` | adapted as thin iframe/new-window wrapper, no duplicate task-list UI | `internal/platform/taskqueue`, `internal/platform/scheduler`, `internal/jobs`, `internal/bootstrap`, `internal/module/queuemonitor`; frontend queue monitor contract test | login log worker path + full smoke queue monitor read-only probe | architecture + contract + smoke matrix | asynqmon is read-only and must be re-tested on Asynq upgrades; future multi-worker scheduler lock/outbox still planned |
| system settings | implemented: REST init/list/create/update/status/delete, enum-backed value type dict, typed value validation, Redis setting-cache invalidation, legacy queue monitor setting excluded | adapted to Go REST API | `internal/module/systemsetting`, `internal/enum`, `internal/dict`, `internal/validate`, `internal/server`, `internal/bootstrap`; frontend typecheck/lint | full smoke probes init/list shape | contract + foundation plan + smoke matrix | `devtools_queue_monitor_queues` old row should be soft-deleted; do not turn generic settings into a dumping ground |
| upload config | implemented: REST upload-drivers/upload-rules/upload-settings, VAULT_KEY secretbox, enum/dict/validate, setting exclusive enable transaction, route permission + operation log metadata | adapted to Go REST typed API/components | `internal/module/uploadconfig`, `internal/platform/secretbox`, `internal/enum`, `internal/dict`, `internal/validate`, `internal/server`, `internal/bootstrap`; frontend typecheck/lint | full smoke probes init/list always; disabled temp write probe only when VAULT_KEY exists | contract + upload foundation spec/plan + smoke matrix | config supports cos/oss records; runtime default is COS-only |
| upload runtime/token | implemented: `POST /api/admin/v1/upload-tokens`, COS-only STS signing, server-generated key, folder/ext/size/rule validation, bearer-token current-user capability with no RBAC button rule and no OperationLog metadata, no legacy fallback | adapted: shared upload client uses Go REST + `cos-js-sdk-v5` only; OSS runtime throws explicit unsupported error | `internal/module/uploadtoken`, `internal/platform/storage/cos`, `internal/config`, `internal/bootstrap`, `internal/server`, `internal/validate`; frontend `src/api/system/uploadToken.ts`, `src/lib/upload/uploadClient.ts` | full smoke token shape probe skips when `COS_STS_ENABLED=false`, otherwise validates shape only | contract + architecture + smoke matrix | no OSS runtime; no server-side upload; real COS requires enabled COS setting + `COS_STS_ENABLED=true` |
| payment domain rebuild | implemented: project-native `internal/module/payment` owns channels/configs/orders/events, Alipay web/H5 create/pay/result/cancel/notify, and `payment:close-expired-order:v1` + `payment:sync-pending-order:v1`; old pay/wallet/reconcile/refund modules are retired | adapted: frontend uses `src/api/payment/*` and `views/Main/payment/*`; legacy pay/wallet API and view directories are removed | `internal/module/payment`, `internal/platform/payment/alipay`, `internal/module/crontask`, `internal/bootstrap`; frontend payment Vitest/typecheck/build | full smoke covers payment channel/order/event probes and runtime menu no longer exposes old pay/wallet | payment rebuild spec/plan + admin API contract + smoke matrix | Alipay only; no wallet/refund/reconcile/WeChat; `orders/order_items` are not dropped until separately proven payment-only |
| realtime / WebSocket / AI streaming | partially implemented: gorilla/websocket thin wrapper, authenticated admin ws route, path-scoped browser cookie auth for `/api/admin/v1/realtime/ws`, explicit Origin allowlist, local connection manager, bounded send queue, read/write pump, connected event, ping/pong, identity topic subscribe whitelist, local/no-op/redis Publisher boundary, Redis Pub/Sub notification fan-out, AI chat `ai.response.*.v1` publication, typed REALTIME config, explicit disabled 503 | adapted baseline plus AI runtime: Vue client uses Go WS URL/envelope, removes legacy `/api/admin/WebSocket/bind`, message bus accepts `ai.response.start/delta/completed/failed/cancel.v1`, and AI chat client combines WebSocket acceleration with REST event catch-up | `internal/platform/realtime`, `internal/module/realtime`, `internal/module/aichat`, `internal/bootstrap`, `internal/config`, `internal/server`; frontend realtime/AI stream Vitest + vue-tsc | basic smoke covers backend connect/ping/pong; full smoke covers AI runtime read probes; tests cover subscribe reject, disabled route, local/noop/redis publisher selection, browser cookie auth/origin policy, frontend URL/envelope cleanup, AI WebSocket event merge/dedupe | architecture + realtime contract + smoke matrix + frontend realtime spec/plan | no arbitrary business topic permission yet; AI response events are first-version envelopes only; no SSE or browser event-source runtime, no Redis Stream replay, no real provider E2E unless separately configured; ticket auth still planned for cross-domain/gateway cases |

## Current verified RBAC loop

```text
login -> AuthToken -> users/me -> users/init
permission create DIR/PAGE/BUTTON
role update grants PAGE/BUTTON
users/init returns temporary router + buttonCodes
role restore
permission subtree delete
users page-init + users list
logout
```

Verification command:

```powershell
powershell -ExecutionPolicy Bypass -File .\scripts\basic-admin-smoke.ps1 -Account 15671628271 -Password 123456
powershell -ExecutionPolicy Bypass -File .\scripts\full-admin-smoke.ps1 -Account 15671628271 -Password 123456
```


